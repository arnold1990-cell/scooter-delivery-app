import AdminPageTitle from './AdminPageTitle';

export default function AdminSettingsPage() {
  return (
    <AdminPageTitle title="Settings">
      <p className="rounded border border-dashed border-slate-300 bg-slate-50 p-4">Configure admin application settings and operational preferences.</p>
    </AdminPageTitle>
  );
}
